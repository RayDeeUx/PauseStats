name: Build Mod

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    uses: geode-sdk/build-actions/.github/workflows/build-multiplatform.yml@main
    with:
      mod-id: jotabelike.pausestats