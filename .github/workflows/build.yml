name: Build Mod

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    uses: geode-sdk/workflows/.github/workflows/build-multiplatform.yml@main
    with:
      mod-id: jotabelike.pausestats